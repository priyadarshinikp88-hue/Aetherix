import axios from "axios";

const TOMORROW_API_KEY = process.env.TOMORROW_API_KEY;

export const getWeather = async (lat, lon) => {
  // We'll implement this after obtaining your Tomorrow.io API key.
};